#pragma once

using namespace godot;

enum Ranges {
	CLOSE,
	MEDIUM,
	LONG,
	SUPER_LONG,
};

enum Teams {
	WHITE,
	BLACK,
	RED,
};

enum Layers {
	GROUND,
	SKY,
	SPACE,
};

enum SubLayers {
	LAYER_1,
	LAYER_2,
	LAYER_3,
	LAYER_4,
	LAYER_5,
};